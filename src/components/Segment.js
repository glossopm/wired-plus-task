import * as React from "react";
import Container from "@mui/material/Container";
import Typography from "@mui/material/Typography";

const Segment = () => {
  return (
    <Container>
      <Typography component="div">Segment Visualisation</Typography>
    </Container>
  );
};

export default Segment;
